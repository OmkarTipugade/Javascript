const arr = [1,3,4,5]

const arr2 = ["ninja","sumoo"]

const myarr = new Array(4, 5,6, 8,9)

console.log(`A ${arr} ${arr2} ${myarr}`)

console.log(`B ${arr.slice(0,2)} ${arr}`)

console.log(`C ${arr.splice(0,2)} ${arr}`)

